// get the detail data after creation

console.log('get the detail data after creation');
console.log('---------------------------------');

var detail = new Detail();

detail.fetch({
  success: function(model, response, options) {
    console.log('detail data: ', model.toJSON());
  },
  error: function(model, response, options) {
    console.log('error: ', response);
  }
});

